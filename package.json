{
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  },
  "scripts": {
    "build": "vite build",
    "deploy": "npx vite build --base=/threeSBML/ && xcopy /E /I dist ..\\deploy\\ && cd ..\\deploy && git add . && git commit -m \"deploy 0.2.1\" && git push origin gh-pages"
  }
}
