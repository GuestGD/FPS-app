{
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "rollup-plugin-gzip": "^4.1.1",
    "vite": "^7.0.6",
    "vite-plugin-compression": "^0.5.1"
  },
  "scripts": {
    "build": "vite build",
    "deploy": "npx vite build --base=/FPS-app/ && xcopy /E /I dist ..\\deploy\\ && cd ..\\deploy && git add . && git commit -m \"deploy 0.1.7\" && git push --force origin gh-pages"
  }
}